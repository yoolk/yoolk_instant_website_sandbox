<h1 class="page-title">services</h1>

{% for category in listing.service_categories %}
  {% for service in category.services %}
    <div class="catalog">
      <a href="/services/kh12806-office-rental-services/59-ground-floor">
        <h2 class="catalog-title">{{ service.name }}</h2>
      </a>
      <div class="catalog-body">
        <div class="catalog-image">
          {% if service.photos.first != blank %}
            <a href="#" class="cboxElement">
              {{ service.photos.first.attachment_meta.large.url | image_tag }}
            </a>
          {% endif %}
        </div>
        <ol class="catalog-thumbnails">
          {% for photo in service.photos %}
            {% if photo != blank %}
              <li class="catalog-thumbnails-image" data-photo-url="#">
                <a class="active" href="" rel="" title="" >
                  {{ photo.attachment_meta.thumbnail.url | image_tag }}
                </a>
              </li>
            {% endif %}
          {% endfor %}
        </ol>
        <div class="catalog-info">
          <ul class="catalog-info-specifications list-dotted">
            {% for feature_hash in service.properties %}
              {% for feature in feature_hash %}
                <li class="clearfix service-list">
                  <span class="ellipsis">{{ feature[0] }}</span><span class="ellipsis">{{ feature[1] }}</span>
                </li>
              {% endfor %}
            {% endfor %}
          </ul>
        </div>
        <div class="clear"></div>
      </div>
    </div>
  {% endfor %}
{% endfor %}